kind = "web"
previewPath = "/backup/"
title = "Quizmi — Study Reimagined (Backup)"
version = "1.0.0"
id = ".migration-backup/artifacts/questly"
router = "path"

[[services]]
name = "web"
paths = [ "/backup/" ]
localPort = 20699

[services.development]
run = "sh -c 'sleep infinity'"

[services.production]

[services.production.build]
args = ["sh", "-c", "echo skip"]

[services.production.run]
args = ["sh", "-c", "sleep infinity"]

[services.production.run.env]
PORT = "20699"
NODE_ENV = "production"

[services.env]
PORT = "20699"
