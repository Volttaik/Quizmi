{
  "name": "questly-android",
  "version": "1.0.0",
  "description": "Questly Android app via Capacitor",
  "private": true,
  "scripts": {
    "build": "echo 'Web assets ready'",
    "cap:add": "npx cap add android",
    "cap:sync": "npx cap sync android",
    "cap:build": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
